{
  "name": "selector.js",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "mocha src/test.js",
    "dev": "nodemon --exec npm run test --ext js,json,pegjs --watch src/"
  },
  "dependencies": {
    "pegjs": "^0.10.0",
    "redent": "^2.0.0",
    "strip-indent": "^2.0.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "jsdom": "^11.3.0",
    "mocha": "^4.0.1"
  }
}
